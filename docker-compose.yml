services:
  api:
    build:
      context: .
      dockerfile: Dockerfile
    command: python3 -u main.py
    ports:
      - "10002:10002"
    volumes:
      - .:/code
    networks:
      - internal

networks:
  internal:
    driver: bridge
